# AGENTS.md

## 规则优先级

* 发生冲突时，依次遵守：不可绕过的安全、权限、真实性和强制性外部约束；用户当前明确要求及已确认决定；当前目录适用的项目规则、声明配置和工具配置；项目现有架构、目录、命名、格式和实现惯例；本文件的默认约定和通用工程最佳实践。
* 更具体范围内的规则优先于更广范围的规则。
* 用户明确要求修改的接口、协议、配置、格式或行为属于本次任务目标；未要求修改的外部契约、数据格式、持久化结构和公共行为应保持有效。
* 不得通过重新解释需求绕过用户明确要求。

## 项目归属

* 第一次修改项目代码时，应确认该项目是用户自有项目还是第三方项目；归属已由用户或上下文明确时不得重复询问，只读分析时无需询问。
* 用户自有项目和从零开发项目默认采用本文件的全部规则。
* 第三方项目优先遵守其既有契约、架构、工具链和维护要求；与本文件默认规则冲突时，应说明差异，并采用对既有契约、数据和维护流程影响更小的方案。
* 用户明确要求第三方项目采用某项规则时，应按照用户要求执行。

## 任务范围与编码前准备

* 严格按照用户明确需求工作，不擅自增加、删除、缩小或重新定义任务目标。
* 不添加用户未要求的独立功能、兼容逻辑、配置项、扩展点或外部能力，不为没有现实依据的未来需求提前实现功能。
* 目标是产出正确、完整、清晰、可维护且能够真实运行的工程实现。
* 修改范围由任务目标约束，并由根本原因、受影响链路和项目整体一致性决定；完成需求所必需的调用方、共享逻辑、测试、配置和文档更新属于任务范围，不得误判为擅自扩大范围。
* 不借机改造无关模块；发现无关问题时只说明，不擅自处理。
* 存在客观限制时，应说明限制、影响和实际可交付范围。
* 实现前先将任务转化为明确且可验证的目标，并阅读与任务直接相关的入口、调用链、数据结构、配置、测试和文档。
* 修改共享代码时，应检查主要调用方和依赖方向；修改公共接口时，应检查定义、调用方、测试、示例和文档；修改数据结构时，应检查生产者、消费者、序列化和持久化位置。
* 能通过代码、配置、测试、文档或运行结果确认的信息，应先自行确认，不得凭经验猜测，也不得询问能够通过当前项目自行确认的问题。
* 关键假设必须来源于已知事实，不得替用户决定需求范围。
* 无法确认且会显著影响范围、行为、数据、安全、权限、成本或不可逆操作时，应先询问用户；低风险、局部且可逆的歧义，应采用符合项目整体约定的合理解释。
* 多步骤任务开始前应给出包含验证步骤的简短计划；简单且可以直接完成的任务无需形式化计划。

## 设计、实现与模块

* 应从项目整体理解当前功能的职责、边界和依赖关系；项目内已有实现符合当前需求和本文件规则时应优先复用，没有合适的既有方式时再采用符合当前项目规模和领域特点的通用工程实践。
* 默认选择符合项目最佳实践且能够解决根本问题的方案；最佳实践应服务于正确性、清晰性、可维护性和合理扩展性，不机械套用设计模式、框架或分层结构，也不以最小修改、修改行数、文件数量或 diff 大小作为实现质量标准。
* 不得为了减少改动、避免更新调用方、测试或文档而破坏模块职责、项目一致性，或保留会造成职责混乱、重复路径或错误依赖方向的接口。
* 如果最佳实践方案需要扩展到当前受影响链路之外、形成大范围跨模块或架构级重构、引入新的依赖或工具链，或相对于当前需求明显增加工作量和验证成本，应在实施前说明必要性、影响和预计范围，并取得用户确认。
* 局部修补会保留错误结构、制造重复或增加技术债时，应在与当前任务存在真实因果关系的范围内重构直接受影响的模块和调用方，优先解决数据模型、职责划分、依赖方向和抽象边界中的根本问题，不得继续堆叠条件判断、特殊分支或旁路实现。
* 同一业务规则应具有唯一且明确的实现位置，同一事实应具有唯一权威来源，不得重复维护。
* 新实现应与项目中同类功能的数据模型、接口风格和错误模型保持一致；同类已有实现违反本节规则时，应在当前受影响范围内统一修正。
* 设计应解决当前需求所代表的完整问题，不得只适配单一输入、当前示例或唯一调用方式。
* 应考虑已有需求、数据模型、调用方式或同类实现能够证明的变化方向，但不得为没有现有依据的远期需求引入复杂扩展体系。
* 抽象、分层或新模块必须降低真实复杂度、消除重复或稳定边界；已有多个实现或明确变化点时，应使用合理抽象统一管理。
* 只有一个实现且没有变化依据时，不得强行创建空接口、空基类或策略体系；是否需要抽象应依据职责边界、重复和变化点判断，不能仅凭调用方数量决定。
* 不得创建只转发调用且不改善职责或边界的包装层，不得在没有当前需求或现有变化依据时将常量和行为配置化，也不得预先实现无实际用途的插件系统或扩展点。
* 对外接口应表达稳定业务含义，不暴露不必要的内部实现细节；除非外部契约或调用方确实需要，内部实现变化不应传播到无关模块。
* 项目 formatter、lint、类型检查和构建配置优先于个人风格偏好；项目没有明确规范时采用对应语言的 Google Style Guide，没有适用指南时采用该语言官方推荐规范。
* 新增代码应使用当前项目所支持版本中的现代稳定特性，不使用已废弃、不推荐或仅为项目不再支持的旧版本兼容而存在的写法，也不得突破项目声明的最低运行版本。
* 命名应准确表达职责、数据含义和业务概念，接口命名应表达稳定语义而非当前实现方式。
* 函数、类和模块应职责清晰且集中；不得为减少文件数量堆积无关职责，不得创建职责模糊的公共工具模块，也不得以复杂表达式、隐式行为或过度压缩追求短代码。
* 现有模块职责不合适时，应创建边界清晰的新模块；拆分应依据职责、变化原因和依赖关系，而非单看文件大小。
* 不得创建循环依赖、隐式全局状态或不可控的模块级可变对象；初始化、资源获取和副作用应位于清晰且可控的入口。
* 不得为套用风格重排或格式化无关代码，不得保留无用导入、变量、参数、类型或不可达分支。

## 兼容性

* 用户自有项目和从零开发项目默认不保留向后兼容；接口、参数、配置、输出或数据结构发生修改时，不保留旧版本入口。
* 未明确要求时，不添加 alias、shim、fallback、迁移适配、双路径或旧格式识别，也不同时维护新旧接口、配置或行为。
* 内部调用方应直接更新到当前实现；仅验证废弃行为的旧测试、旧文档应更新或删除，旧配置键、旧字段和旧导入路径不得继续作为隐藏兼容入口。
* 同一概念演进时继续使用稳定原名，不因历史版本添加 `v1`、`v2`、`new`、`old` 或 `legacy` 后缀；只有多个版本确实需要同时存在时，才使用明确的版本命名。
* 不要求向后兼容不代表可以修改与当前任务无关的公共接口，也不代表可以无提示删除、覆盖或损坏持久化数据。
* 用户明确要求兼容时，只实现指定对象和范围内的必要兼容；必要且复杂的兼容逻辑应集中隔离，由兼容层单向调用当前实现，不得让当前实现依赖兼容层。
* 第三方项目默认遵循其既有兼容策略，除非用户明确要求改变。

## 外部源代码

* 不得直接修改依赖包、上游库、运行时、编译器、工具安装目录、`site-packages`、依赖缓存、包管理器缓存或其他安装产物中的源代码。
* 不得直接修改自动生成的源代码；任务需要变更时，应修改生成器并通过它更新生成产物。
* 外部源代码存在问题时，应优先使用项目内正式扩展机制，例如封装、组合、继承、适配器、代理、官方扩展点，或在项目内创建职责明确的独立文件或替代模块。
* 不得将修改外部源代码作为默认方案，也不得复制与当前问题无关的大量上游代码。
* 确认只能通过修改上游源代码解决时，应在执行前说明原因、影响和无法采用项目内方案的依据，并取得用户确认；临时补丁不得伪装成正式、稳定或已被上游支持的方案。

## 环境与依赖

* 项目运行时、构建方式和依赖管理应遵循用户要求和项目声明配置；Python 项目同时遵循下一节的专门规则。
* 不引入当前需求不需要或与现有依赖功能重叠的依赖，不得为了少写代码而引入主要能力与当前需求无关的依赖，也不得为了避免合理依赖而重复实现成熟且复杂的通用能力。
* 新增依赖前应评估必要性、维护状态、许可证和适用范围。
* 不得手动编辑包管理器或构建工具生成的锁文件；需要变化时应通过对应工具重新生成。
* 不得手动修改依赖缓存、构建缓存或安装目录解决问题。
* 修改依赖、运行时或工具链时，应同步更新配置、脚本、文档和持续集成设置。
* 项目 CLI、版本控制、容器和构建系统应使用其官方命令、声明配置和既有工作流，不得同时保留多个功能重叠的环境或依赖管理方式。

## 命令执行

* 运行项目代码、测试、构建、服务、数据处理或其他可能持续的任务时，无论当前是本机还是远程环境，默认在 `tmux` 会话中执行；只有用户明确要求不使用 `tmux` 时才可直接运行。文件读取、文本搜索、状态查询等短时只读检查不受此限制。
* 使用 `tmux` 启动任务后，仍应检查任务的最终状态和完整输出，不得仅因任务已经启动就声称运行成功或验证通过；`tmux` 不可用时，应说明原因并请求用户决定，不得静默改为直接运行。
* `tmux` 仅用于保护 Agent 的执行过程，不属于项目面向用户的运行方式；README、帮助文本和使用文档应保留项目原生命令，不得要求用户通过 `tmux` 运行，除非用户明确要求在文档中介绍 `tmux`。

## Python

* Python 版本优先遵循用户明确指定，其次遵循项目声明；两者均未指定时默认使用 Python 3.12，新建 Python 项目必须显式使用 Python 3.12。
* 项目已声明其他 Python 版本时，涉及版本或环境修改前应确认是否迁移；未确认迁移时不得使用超出项目声明版本的语法或标准库能力。
* Python 环境、依赖、虚拟环境、脚本和模块执行统一使用 `uv`；任务涉及这些内容或持续集成时，必须使用 `uv`。
* 需要修改原有 Conda、Poetry、Pipenv、裸 `pip` 或其他环境配置时，应完整迁移到 `uv`，并同步修改依赖声明、环境文件、脚本、文档和持续集成配置，不得保留两套环境管理方式。
* 除非 `uv` 客观上无法满足任务，否则不得改用其他工具；无法满足时应说明具体阻碍，不得自行切换。只有用户明确要求时，才可使用其他 Python 环境或依赖管理工具。
* 提供 CLI 的 Python 项目，入口模块使用 `cli.py` 并通过 `[project.scripts]` 暴露，不使用 `main.py`。
* 采用 Python 3.12 的项目应使用其现代稳定语法和标准库能力，不使用仅为兼容旧 Python 版本而存在的写法。
* Python 项目默认使用 Ruff 进行格式和静态检查，使用 Pyright 进行类型检查；项目已有更严格的检查工具时可以同时使用，但不得因此省略 Ruff 或 Pyright。
* Ruff 和 Pyright 的项目配置应作为 Python 代码检查依据；本文件不重复规定能够由它们可靠检查的代码细节。

## 编辑器、诊断与问题修复

* 默认开发编辑器为 Visual Studio Code；检查代码时应优先参考编辑器可见的 Problems、错误和警告，无法直接读取时应运行能够产生同等诊断的项目工具。
* 应根据诊断来源运行对应的 formatter、lint、类型检查、编译或构建工具，不得仅凭肉眼阅读代替可用的自动检查。
* 用户自有项目中，所有修改文件的错误和警告必须清零，包括原有红色和黄色诊断，不得以历史问题为由保留；该要求仅限修改文件，若修复需要修改任务无关的文件、公共接口或外部契约，或需要新增依赖，应先询问用户。
* 第三方项目中，必须解决本次修改引入或直接触及的错误和警告；不要求清理无关历史诊断，也不得因此擅自扩大修改范围。
* 不得通过关闭扩展、禁用或弱化规则、降低诊断等级、隐藏 Problems 或绕过检查制造无报错状态。
* 编辑器未显示问题不代表代码已经通过运行验证。
* 遇到类型、静态检查、测试或运行错误时，应定位并修复根本原因，不得只处理表面现象而保留错误结构。
* 类型声明必须反映真实运行时行为。用户自有项目中，不得用 `Any`、`cast`、`type: ignore`、`noqa`、放宽类型声明或关闭检查掩盖能够准确表达和修复的问题。
* 不得通过吞异常、返回与设计不符的默认值、空值或成功状态掩盖错误；明确属于接口设计的默认行为不在此限。
* 第三方项目只有在第三方接口的实际行为或类型与其声明不一致，或检查工具存在可验证缺陷且无法在项目内修复时，才允许最小范围例外，并说明原因、影响、范围和无法根治的依据。
* 工具误报不能仅凭主观判断认定，应提供可验证依据；能通过调整真实实现解决的问题不得标记为误报。
* 只捕获当前层能够正确恢复、转换或补充上下文的错误；无法正确处理时应保留原始上下文并继续向上传递，不使用宽泛捕获代替输入校验、状态判断或正确控制流。
* 错误信息应能够定位失败阶段和原因，并遵守本文件的敏感数据保护规则。

## 配置

* 配置位置和格式优先遵循框架、协议、工具和现有项目要求；已有合适配置时应直接扩展，不得重复创建配置来源，也不因个人偏好修改工具强制要求的格式。
* 项目自行设计且没有外部格式约束的配置默认使用 TOML，并按分层覆盖加载。
* 配置优先级从低到高为：包内默认配置、外部配置文件、环境变量、命令行参数。
* 包内默认配置放在 `resources/` 目录，随包安装并作为唯一出厂默认值；外部配置放在项目根目录 `configs/`，只保存对默认值的覆盖项，不得同时维护两份完整默认配置。
* 读取、合并和校验配置的代码放在包内 `config` 模块，与配置文件分开。
* 外部配置应递归覆盖默认配置，嵌套字典逐层合并而非整体替换；其中的相对路径相对于该配置文件所在目录解析。
* Python 包内 `resources/` 下的非 Python 文件通过 `importlib.resources` 访问。
* 需要由部署环境、用户或运行策略调整的值不应硬编码；固定业务规则不应无理由配置化，也不为未提出的灵活性新增配置项。
* 同一概念只能有一个权威配置来源；相同类型的配置应使用统一位置、结构和键名，不得在多个文件中重复保存同一事实。
* 新配置结构确定后只接受当前规范，默认不保留旧配置键、旧字段或旧格式兼容路径。
* 配置变化必须同步更新读取代码、校验、示例和文档。
* 密钥、令牌等敏感配置通过环境变量提供，不写入配置文件或仓库。
* 配置缺失时不得通过静默默认值掩盖问题，除非默认值属于明确设计；用户显式指定的配置文件不存在时应直接报错，不得静默回退。

## 日志与文字

* 日志应复用项目现有机制、配置入口、事件命名和上下文字段；业务模块不得自行配置全局日志系统，日志系统只能在应用启动、命令入口或测试初始化边界统一配置。
* 同类日志应使用稳定一致的字段和事件命名。
* 不记录密码、令牌、密钥、认证信息、完整隐私数据、无必要的大型请求体、完整数据集或敏感原文。
* 不得在仓库根目录散落运行日志；创建持久化日志时应同步配置忽略规则，运行日志不得提交到版本控制。
* 终端输出、程序界面文字、日志和异常信息默认使用英文；注释、文档注释、README、设计说明和开发文档默认使用中文。
* 命令、路径、配置键、文件名、API 名称和专业技术标识保持原样。
* 用户、项目、协议或外部接口明确要求其他语言时遵循其要求；不得在同一类文本中无原因混用中英文。

## 文档

* 代码、配置、接口或用户可见行为发生变化时，必须同步更新直接相关文档；文档更新属于实现的一部分，不得作为可选收尾工作。
* 修改公共 API、命令参数、配置结构、数据格式、运行方式、依赖管理、错误行为、架构或模块职责时，应同步更新对应的 API 文档、帮助文本、README、示例、schema、部署和排查说明或设计文档。
* 代码中的注释和文档必须与实际实现一致，不得保留描述旧实现、参数、路径或行为的内容；删除功能时应同步删除只服务于该功能的文档和示例。
* 文档中的命令必须符合当前工具链和实际运行方式。
* 任务涉及代码行为时，仅修改文档不足以证明代码行为正确。

## 安全与数据

* 不得在代码、配置、测试、日志或文档中写入真实密钥、令牌、密码或隐私数据。
* 外部输入应在进入受信任代码前进行必要校验；SQL 应使用参数化查询，Shell 调用应优先使用参数列表并避免将输入拼入命令字符串，确需 Shell 语法时应采用对应的安全引用机制。
* 文件路径应校验允许访问的范围，模板应使用对应上下文的转义机制，不得仅依赖简单替换或主观判断。
* 不得通过关闭证书校验、权限检查、安全限制、静态检查、测试或验证工具解决问题。
* 涉及删除、覆盖、迁移或重写持久化数据时，必须在执行前说明目标、范围和不可逆影响；破坏性操作应优先采用可验证、可备份或可回滚方案，未经明确授权不得执行不可逆数据破坏操作。
* 不得伪造权限、认证、外部服务或真实集成已经完成。
* 测试数据和示例数据不得包含可识别的真实敏感信息。

## 修改与清理

* 修改或删除共享代码、公共符号前，应检查所有直接使用方以及定义、测试、文档和配置引用，不得仅因当前文件没有引用就认定可以删除。
* 移动代码时，应同步更新导入、配置、测试、文档和构建引用。
* 替换实现时，应删除失效的旧实现、临时代码路径和兼容路径，并同步更新或删除其专属测试、文档、配置和依赖；不得保留新旧双路径或只为旧测试通过而存在的行为。
* 不得顺带删除与当前任务无关的原有死代码；第三方项目中发现此类代码时，应说明位置、判定依据和影响，不擅自处理。
* 本次新增或修改的代码不得留下临时文件、调试输出或实验逻辑；清理后应重新运行相关检查，确认不存在失效引用。

## 测试与验证

* 修复缺陷时，应尽可能先复现问题，并添加修复前失败、修复后通过的回归测试；无法复现或无法添加测试时，应在最终说明中写明原因。
* 测试应覆盖与本次修改相关的正常路径、边界条件和失败路径，验证稳定行为和业务规则，不得只验证当前示例输入。
* 修改输入校验时，应同时测试合法和非法输入；修改数据模型时，应测试主要生产和消费路径；修改共享逻辑时，应测试主要调用场景。
* 修改外部行为时，应更新不再符合需求的旧测试，不得为保留未要求的旧行为而迁就旧测试。
* 不得削弱断言、跳过有效测试或删除有效测试制造通过结果；mock 不得替代本次修改所依赖的关键真实边界或集成路径。
* 优先运行与修改直接相关的测试；修改共享代码、公共接口、核心流程或高风险逻辑时，应进一步运行相关模块、调用方或集成测试。
* 修改 Python 代码后，应运行项目配置的 Ruff 和 Pyright；项目配置了 formatter、lint、类型检查、测试或构建命令时，应运行所有适用命令。
* formatter、lint、类型检查、测试和构建不能互相替代；无法运行必要检查时，应说明原因并采用可行的替代验证。
* 不得在没有验证依据时声称实现正确、完整或已经通过，不得虚构命令输出、测试结果、运行状态、性能数据或外部服务响应。
* 新增或修改的实现不得留下未完成的 `TODO`、伪实现、占位返回值或未实现分支，必须支持真实输入和实际运行场景，不得依赖测试代码、fixture 名称、测试环境特征、硬编码样例或测试专用路径。

## 完成前检查与最终说明

* 完成前应按本文件逐项确认实现、清理、诊断、测试和文档要求均已满足，并已运行与修改类型和风险相匹配的检查。
* 涉及生成文件时，应确认文件通过对应工具更新。
* 最终回复应说明修改内容和实现理由；存在架构、模块边界或公共接口调整时，应说明影响。
* 最终回复应列出实际运行的测试和检查；未执行的必要检查或命令必须明确标记为未执行并说明原因，仅通过代码阅读得出的判断不得描述为运行验证。
* 最终回复应说明已知限制、外部依赖和仍需用户决定的事项。
* 最终说明的详细程度应与任务复杂度和风险相匹配。
